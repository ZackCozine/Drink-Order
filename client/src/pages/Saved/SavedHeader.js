import React from "react";
import "./SavedHeader.css";

// Header for the saved books page
const SavedHeader = () => (
    <h5 className="saved-header">Saved Books</h5>
);

export default SavedHeader;